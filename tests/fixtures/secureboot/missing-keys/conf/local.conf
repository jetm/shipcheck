# Signing configured but key files do not exist
MACHINE = "genericx86-64"
DISTRO = "poky"
IMAGE_CLASSES += "uefi-sign"
SECURE_BOOT_SIGNING_KEY = "${TOPDIR}/keys/nonexistent.key"
SECURE_BOOT_DB_KEY = "${TOPDIR}/keys/nonexistent-db.key"
