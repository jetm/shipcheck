# Yocto build configuration with Secure Boot signing enabled
MACHINE = "genericx86-64"
DISTRO = "poky"
IMAGE_CLASSES += "uefi-sign"
SECURE_BOOT_SIGNING_KEY = "${TOPDIR}/keys/db.key"
SECURE_BOOT_SIGNING_CERT = "${TOPDIR}/keys/db.crt"
