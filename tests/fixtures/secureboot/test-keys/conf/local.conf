# Yocto build configuration with signing but using test keys
MACHINE = "genericx86-64"
DISTRO = "poky"
IMAGE_CLASSES += "uefi-sign"
SECURE_BOOT_SIGNING_KEY = "${TOPDIR}/keys/test-signing-key.pem"
SECURE_BOOT_DB_KEY = "${TOPDIR}/keys/development-db.key"
